function checkGreaterNum(x, y) {
    if (x > y) {
      console.log(`${x} is greater than ${y}`);
    } else {
      console.log(`${y} is greater than ${x}`);
    }
  }
  
  let x = 5;
  let y = 10;
  checkGreaterNum(x, y);
  
  x = 7;
  y = 3;
  checkGreaterNum(x, y);
  